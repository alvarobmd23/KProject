from django.contrib.auth.decorators import login_required
from django.urls import path

'''
from .views import ()

app_name = 'persons'


urlpatterns = [
    # Document_Type
    path('persons/',
         login_required(document_type_list), name='persons'),
    path('persons/<int:pk>/',
         login_required(document_type_detail), name='persons_detail'),
    path('persons/add/',
         login_required(Document_Type_Create.as_view()), name='persons_add'),
    path('persons/edit/<int:pk>/',
         login_required(Document_Type_Update.as_view()), name='persons_edit'),
    path('persons/delete/<int:pk>/',
         login_required(Document_Type_Delete.as_view()), name='persons_delete'),


]
'''
